/**
 * Tones are the shared vocabulary for "how is this doing?" across every
 * primitive. A component takes a tone; it never takes a colour.
 */
export type Tone = 'ok' | 'busy' | 'warn' | 'bad' | 'idle' | 'accent';
